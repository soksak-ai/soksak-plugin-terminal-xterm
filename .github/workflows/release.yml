name: release
on:
  workflow_dispatch:
concurrency:
  group: release-${{ github.repository }}
  cancel-in-progress: false
permissions:
  contents: read
jobs:
  release:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-24.04
    defaults:
      run:
        working-directory: soksak-plugins/soksak-plugin-terminal-xterm
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          path: soksak-plugins/soksak-plugin-terminal-xterm
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          repository: soksak-ai/soksak-contract-plugin-terminal
          ref: 78a09a60b569b9b6cf83a6f3c72086948e650ff7
          path: soksak-contracts/soksak-contract-plugin-terminal
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          repository: soksak-ai/soksak-kit-plugin-terminal
          ref: 2c921e07ac0b626ece438fc69982a28bba413ce4
          path: soksak-kits/soksak-kit-plugin-terminal
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          repository: soksak-ai/soksak-spec
          ref: 23a941a99f037f48120243e5724a73284e6cfb30
          path: soksak-plugins/soksak-plugin-terminal-xterm/.dependency/soksak-spec
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          repository: min-median-max/xterm-addon-webkit-ime
          ref: 488de7fa0585aa251b0520785d687b2be2d1e4d5
          path: externals/xterm-addon-webkit-ime
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: "22.12.0"
      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1
        with:
          version: "10.30.3"
      - name: Build the exact public spec
        working-directory: soksak-plugins/soksak-plugin-terminal-xterm/.dependency/soksak-spec
        run: |
          pnpm --config.node-linker=hoisted --config.symlink=false install --frozen-lockfile
          pnpm build
          test -z "$(find . -type l -print -quit)"
      - name: Verify and build the plugin
        run: |
          pnpm --dir frontend install --frozen-lockfile
          pnpm --dir frontend typecheck
          pnpm --dir frontend test
          pnpm --dir frontend build
          git diff --exit-code -- main.js
      - name: Build and validate release documents
        run: |
          node .dependency/soksak-spec/packages/plugin-spec/release-template/build-release.mjs --commit "${{ github.sha }}" --out dist-release
          node .dependency/soksak-spec/packages/plugin-spec/bin/validate.mjs release dist-release/release.json
          node .dependency/soksak-spec/packages/plugin-spec/bin/validate.mjs conformance dist-release/conformance-*.json --release dist-release/release.json --plugin-manifest plugin.json
      - id: release-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1
        with:
          client-id: ${{ vars.SOKSAK_RELEASE_CLIENT_ID }}
          private-key: ${{ secrets.SOKSAK_RELEASE_PRIVATE_KEY }}
          permission-administration: read
          permission-contents: write
      - name: Publish immutable v0.0.1 release
        env:
          GH_TOKEN: ${{ steps.release-token.outputs.token }}
        run: |
          test "$(gh api "repos/${{ github.repository }}/immutable-releases" --jq '.enabled and .enforced_by_owner')" = true
          test "$(find dist-release -maxdepth 1 -type f -name '*.tgz' | wc -l | tr -d ' ')" = 1
          test "$(find dist-release -maxdepth 1 -type f -name 'conformance-*.json' | wc -l | tr -d ' ')" -ge 2
          gh release create v0.0.1 --repo "${{ github.repository }}" --target "${{ github.sha }}" --title v0.0.1 --generate-notes dist-release/*
