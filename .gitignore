# 머신-로컬 dev 마커(추적 금지 — 배포 메타는 별도 파일)
.soksak.json
node_modules

# local-only working notes (never committed)
secret/
dist/
node_modules/

# build-generated (kit path discovery — see build.mjs)
tsconfig.paths.json
